import 'package:flutter/material.dart';

String baseUrl = "https://admin.ticketmaster-et.com/public/storage/";
String apiUrl = "https://api.ticketmaster-et.com/api";

TextStyle kLargeHeader = const TextStyle(
    fontSize: 35, fontWeight: FontWeight.bold, color: Colors.black);

TextStyle kSmallText = const TextStyle(fontSize: 17, color: Colors.green);
TextStyle kTextStyle = const TextStyle(fontSize: 15);

TextStyle kSmallHeader = const TextStyle(
    fontSize: 20, fontWeight: FontWeight.w600, color: Colors.teal);

// var closedHours = DateTime.now().hour > 7 && DateTime.now().hour < 18;
